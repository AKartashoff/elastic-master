package controllers;

/**
 * abstract class for different routing policies.
 * Class has an abstract method which returns the url of the slave server which will execute the ETL
 * it will take arguments (for example CPU load and Memory load) in the future realisation
 */
public abstract class RoutingPolicy{

    //abstract String getNextURL(int cpuLoad, int memoryLoad);
    abstract String getNextURL();
}
